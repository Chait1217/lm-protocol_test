[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200

# Base mainnet – set BASE_MAINNET_RPC_URL in .env (e.g. https://mainnet.base.org)
[rpc_endpoints]
base = "${BASE_MAINNET_RPC_URL}"

[etherscan]
base = { key = "${BASESCAN_API_KEY}" }
